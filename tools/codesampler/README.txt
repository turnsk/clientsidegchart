The code sampler taglet makes it easy to include excerpts from example
programs within javadocs, for example, when discussing an example
program as part of a tutorial embedded in the javadocs.

In Client-side GChart, this tool is used to automatically include the text of
each example application within the "Chart Gallery" embedded within
the Client-side GChart package description.

